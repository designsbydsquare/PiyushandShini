const intro=document.getElementById('intro'),video=document.getElementById('introVideo'),music=document.getElementById('bgMusic'),musicToggle=document.getElementById('musicToggle');
function closeIntro(){intro.classList.add('hidden');document.body.classList.remove('locked');music.play().then(()=>musicToggle.textContent='🔊').catch(()=>{});document.querySelector('.hero .reveal')?.classList.add('visible')}
document.getElementById('skipIntro').onclick=closeIntro;document.getElementById('openInvite').onclick=closeIntro;video.addEventListener('ended',closeIntro);video.addEventListener('loadeddata',()=>intro.classList.add('has-video'));video.addEventListener('error',()=>intro.classList.add('no-video'));setTimeout(()=>{if(video.readyState===0)intro.classList.add('no-video')},1200);
musicToggle.onclick=()=>{if(music.paused){music.play().then(()=>musicToggle.textContent='🔊').catch(()=>{})}else{music.pause();musicToggle.textContent='♫'}};
const weddingDate=new Date('2026-06-30T10:30:00+05:30').getTime(),cd=document.getElementById('countdown');function tick(){let x=Math.max(0,weddingDate-Date.now()),v=[Math.floor(x/86400000),Math.floor(x/3600000)%24,Math.floor(x/60000)%60,Math.floor(x/1000)%60];[...cd.children].forEach((e,i)=>e.querySelector('strong').textContent=String(v[i]).padStart(2,'0'))}tick();setInterval(tick,1000);
const obs=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>obs.observe(e));
const c=document.getElementById('scratchCanvas'),ctx=c.getContext('2d');let draw=false,lx=0,ly=0,s=0;function resize(){let r=c.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);c.width=r.width*d;c.height=r.height*d;ctx.setTransform(d,0,0,d,0,0);let g=ctx.createRadialGradient(r.width*.35,r.height*.25,10,r.width*.55,r.height*.55,r.width*.7);g.addColorStop(0,'#f4d8df');g.addColorStop(.45,'#c57a90');g.addColorStop(1,'#9c536b');ctx.globalCompositeOperation='source-over';ctx.fillStyle=g;ctx.fillRect(0,0,r.width,r.height);for(let i=0;i<180;i++){ctx.fillStyle=`rgba(255,255,255,${.18+Math.random()*.5})`;ctx.beginPath();ctx.arc(Math.random()*r.width,Math.random()*r.height,Math.random()*2.2,0,7);ctx.fill()}ctx.globalCompositeOperation='destination-out'}resize();addEventListener('resize',resize);function pt(e){let r=c.getBoundingClientRect(),p=e.touches?.[0]||e;return{x:p.clientX-r.left,y:p.clientY-r.top}}function move(e){if(!draw)return;e.preventDefault();let p=pt(e);ctx.lineWidth=42;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(lx,ly);ctx.lineTo(p.x,p.y);ctx.stroke();lx=p.x;ly=p.y;if(++s>65){c.style.transition='opacity .8s';c.style.opacity=0;setTimeout(()=>c.style.display='none',800)}}c.addEventListener('pointerdown',e=>{draw=true;let p=pt(e);lx=p.x;ly=p.y});c.addEventListener('pointermove',move);addEventListener('pointerup',()=>draw=false);
document.getElementById('rsvpForm').onsubmit=e=>{e.preventDefault();document.getElementById('formNote').textContent='Thank you! Connect this form to Formspree/Getform to receive RSVPs.'};
